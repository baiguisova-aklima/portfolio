/** @type {import('next').NextConfig} */
const nextConfig = {
  // Конфигурация для красивого URL
  // При деплое на Vercel можно добавить кастомный домен в настройках проекта
}

module.exports = nextConfig

